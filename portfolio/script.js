document.getElementById("contact-form").addEventListener("submit", function(event) {
    event.preventDefault();

    // You can add code to handle form submission, e.g., sending an email or storing data.
    alert("Form submitted!");
});
